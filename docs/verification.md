# 实施验证记录

日期：2026-09-16。

## 环境与结论

- macOS 27.0（26A428），Hammerspoon 1.1.1，单显示器，辅助功能权限已开启。
- 系统开启“按应用程序分组窗口”。实现对重叠缩略图计算可见区域，设置实际鼠标位置并发送悬停事件。
- 发现 AltTab 也使用 Command+Tab，曾抢占焦点。正式实机验收前已退出 AltTab；没有修改其配置。早期带 AltTab 的结果不计入最终验收。
- 退出 AltTab 后，两个 Chrome 同名空白窗口的 3 次切换成功；随后重新执行 20 次连续长按，20 次目标 ID 与实际焦点 ID 全部一致，调度中心均关闭。
- 短按回放触发应用切换，状态回到 idle；没有残留调度中心。
- 额外实机场景：Tab 后反引号返回、打开动画中提前松手均精确命中目标；Esc 取消后焦点保持在原窗口。
- 已安装到 `~/.hammerspoon/Spoons/MissionTab.spoon`，启动块已写入 Hammerspoon 配置并创建备份；已加载运行。

## 自动化验证

通过 `./scripts/test.sh` 在 Hammerspoon 的 Lua 运行时执行，无外部依赖。

| 范围 | 断言数 | 覆盖 |
| --- | --- | --- |
| 纯状态机 | 30 | 长短按边界、Tab 长按、双 Command 聚合、重复键、连续导航、反向、取消、迟到 key-up、其他组合键 |
| 真实控制器 + 模拟系统边界 | 30 | 合成事件标记、立即连续短按、提前松手、反向选择、目标失效、打开/关闭超时、进程替换、外部退出、鼠标接管、Secure Input、stop 清理、打开前取消竞态、打开中停用/重启、诊断结果隔离 |
| AX 适配与几何 | 21 | WindowManager/Dock 两种结构、同名窗口 ID、MRU 关联、动画稳定、缺失进程、分组窗口可见区域、完全遮挡时取消 |

## 已知边界

- 自动实机按键由 Quartz 合成事件驱动；安装后用户实机反馈“短按和长按都正常”，真实物理按键基础交互已确认。
- 多屏、其他 macOS 版本、系统进程实际重启、Secure Input 的真实密码场景尚未实机验证；相关状态清理由模拟边界测试覆盖。
- AXPress 在早期调试中没有建立可靠的精确选窗证据，未用于正式确认路径。
- 完全被其他缩略图遮挡、没有安全可见区域的目标会取消，不猜测点击位置。
- 0.1.0/0.1.1 测试中的鼠标接管会结束会话；0.1.2 已按用户反馈修正为保留 Command 释放监听。系统产生的零位移 mouseMoved 不视为主动鼠标操作。
- AltTab 等工具不能同时接管同一个 Command+Tab。安装 MissionTab 不修改其他工具的登录启动设置。

## 复现

`README.md` 包含安装、停用及诊断命令。实机驱动为 `scripts/live-smoke.lua`，支持连续轮数和额外交互场景。驱动会保留所有计时器并捕获异步异常；鼠标接管或调度中心残留时中止，不继续发送下一轮按键。

## 评审修复

需求评审发现：在已发出打开请求、AX 尚未出现时取消，会过早结束会话。已改为有界等待并关闭随后出现的调度中心；同样覆盖 stop/start 清理，避免重复 toggle。补充回归验证后全部自动断言通过。

项目知识服务在本轮不可用，未宣称完成依赖外部 RULE 的完整规范评审；本地需求、实现、测试及用户实机反馈已核对。

## 0.1.1 导航顺序

按用户反馈改为顺时针 Tab、逆时针反引号，以最近使用的其他窗口为初始项。新增圆周顺序、初始选择与空间顺序分离、循环、单窗口及动画期间缓存输入的验证；81 项自动断言通过。原实机连续测试记录对应 0.1.0；本轮不重复无关的连续切换压力测试。

## 0.1.2

修正鼠标移动导致会话提前结束的问题，并支持在遗留调度中心中按 Command+Tab 退出。按用户要求直接交付，未运行测试或实机验证；此前断言和压力测试数量不代表本版本验收。

## 0.1.3

初始定位改为动画中预定位和跟随，取消 250 ms 固定起步门槛；按用户要求未测试，实际延迟和手感由用户验证。

## 0.1.4

增加应用堆叠组的整体顺时针排序和组内从上到下访问；按用户要求未运行测试，由用户验证。

## 0.2.0 无鼠标交互

当前系统实机调研中，直接 AXPress 为 6/8 命中，退出后按窗口 ID 聚焦为 8/8 命中。严格无鼠标原型仅监听 keyDown、keyUp、flagsChanged，对鼠标 API 设置报错防护；初始选择、Tab、反引号、Shift+Tab、Esc、提前松手、短按、已有调度中心退出共 8 场景各两轮，16/16 通过，鼠标调用违规为 0。测试为合成键盘事件，非人工物理按键。

安装实现补全选中框尺寸跟随、停止和异常时覆盖层清理、目标无法解析时取消。按用户要求，安装版效果由用户试用；未运行旧测试或新增实机回归。原有控制器测试依赖鼠标悬停，不能作为 0.2.0 验证依据。多屏和各应用兼容性未全量覆盖。

## 0.2.1 鼠标接管

用户明确选择移动鼠标退出键盘选择、保留调度中心。实现只监听 mouseMoved，非零位移隐藏选中框；松开 Command 使用系统鼠标选择，再按导航键恢复键盘导航。未运行自动化或实机交互测试，由用户试用验证。

## 0.2.2 鼠标起点与单屏范围

增加按 AXDisplayID 筛选鼠标屏幕（缺少显示器 ID 时使用几何归属），以系统 AX 命中元素确定指向的缩略图；没有命中则从左上方组开始。鼠标接管后再次导航重建范围和起点。完成语法及静态检查，未运行自动化或实机回归，多屏与层叠窗口命中由用户验证。

## 0.2.3 与原生高亮实验

恢复 Command+Tab 长按初始 MRU 选择，候选仍限定鼠标屏幕；鼠标接管后使用鼠标起点。完成语法检查，交互由用户验证。

原生光标模块实验可调用隐藏/显示，系统原生悬停框可见，但调度中心内持续隐藏查询未通过，故未安装该实验。正式版本继续自绘框、不主动移动指针。实验过程中所有临时监听、定时器和光标操作均已结束，最终光标可见，原版运行状态 idle。

## 0.2.4 可见光标与原生高亮

用户确认改用直接移动可见光标，移除自绘框。复用原有安全悬停区域、事件标记和悬停等待，保留当前屏幕与最近窗口起点；鼠标主动接管不回移，键盘退出和停用清理恢复起始位置。完成语法与静态检查，本轮未运行实机交互，由用户验证。

## 0.2.5 退出落点

确认退出并等待焦点稳定后，读取实际前台窗口矩形，将光标移到窗口中心，同时防止完成清理再次回移。取消保持原有恢复规则。完成语法与加载检查，未运行实机交互测试，由用户验证。

## 0.2.6 提前居中

光标居中移到调度中心消失后的首个处理周期，目标聚焦后立即执行。150 ms 仅用于后续焦点诊断，不再延迟移动；每次会话只居中一次。完成语法与加载检查，未进行实机交互测试，由用户验证。

## 0.2.8 鼠标续接与快捷键透传

鼠标命中判断增加明确的命中标志，续接时仅在实际命中窗口的情况下计入首次导航方向；空白处不跳过起点。会话只拦截导航与取消，其他按键原样放行，相关状态机断言已更新。按用户约定未运行自动化或实机交互测试，完成语法与加载检查。

## 0.2.9 空白处选择最近窗口

鼠标续接未命中窗口时，按到候选缩略图矩形的最短距离选择，等距时按中心距离打破平局；不额外推进一项。候选仍限定鼠标屏幕。完成语法与加载检查，交互由用户验证，未运行实机测试。

## 0.2.10 入场悬停刷新

布局稳定后经过 `hoverDelay`，重新读取当前目标坐标并补发一次悬停事件。确认等待此次事件处理，鼠标接管则不补发。控制器模拟系统忽略早期悬停事件：修改前未刷新目标，修改后通过；这验证时序处理，不证明 macOS 原生轮廓的实机效果。

执行 `./scripts/test.sh`：状态机 38 项、控制器 34 项通过；几何测试仍在历史 MRU 断言 `recent other window chooses initial selection` 失败（当前 MRU 选择属于控制器，`MC.order` 仅负责空间排序），未修改该无关测试。控制器测试补齐现有屏幕、窗口查找与窗口矩形接口，并按当前鼠标接管及退出居中行为更新旧断言。未进行实机交互，轮廓显示仍需试用确认。
