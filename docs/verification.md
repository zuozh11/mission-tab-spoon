# 实施验证记录

## 2026-09-20：未分组窗口的应用标识（0.2.32）

- 使用现有调度中心 AX 缩略图坐标和系统窗口列表关联进程；读取应用图标与本地化名称，不枚举所有应用的 AX 窗口。
- 以用户提供的原生分组截图为样式参考：48 点图标画布，中心位于缩略图底边上方 8 点；下方居中显示 13 点系统字体名称。图标资源自带透明留白。
- 三套测试通过（42 / 172 / 249 项断言），新增手势入口、名称与位置、布局移动、窗口关闭、退出清理、分组偏好重读、停用清理和缺失图标覆盖。
- 单屏实机打开原生调度中心，截图确认 Code、IntelliJ IDEA、ChatGPT、Chrome、系统设置和 Apifox 的图标与名称显示；退出后画布已清理。
- 未分组布局不为应用名称预留空间，密集布局底部的文字可能覆盖相邻窗口或程序坞；未更改系统窗口布局。多屏与不同 macOS 版本尚未实机验证，叠加绘制不能保证与系统内部渲染逐像素一致。

## 2026-09-18：最近使用窗口起点修复（0.2.28）

- 用户补充复现：Chrome 有两个窗口时，即使上一个使用的是其他应用，进入调度中心仍选中另一个 Chrome 窗口。
- 旧实现直接使用 `hs.window.list()` 的窗口堆叠顺序；实机读取确认它与 `_orderedwinids()` 相同，但这种排序无法区分实际聚焦与应用连带抬高的其他窗口。
- 回归复现：系统顺序固定为 1、2、3，实际焦点按 1 → 3 → 1 变化；旧选择逻辑错误选择 2，新逻辑选择 3。临时移除历史优先选择后，该断言变红，恢复后通过。
- 三个测试套件通过（42 / 162 / 42 条断言），覆盖跨应用与同应用焦点变化、候选消失、空间顺序继续导航、预览不污染历史、安全输入恢复后仍保留真实顺序及停止监听。
- 已安装并原位加载 0.2.28，实际 AX observer 报告运行中，已记录当前焦点窗口；保留原配置参数。
- 插件启动前的完整历史不可恢复；无记录候选仍按系统顺序补充。真实用户反馈场景和多屏仍需实际使用确认。

## 2026-09-18：切换主线程阻塞修复（0.2.27）

- 用户复现：先无响应，再出现系统切换器，卡顿进入、退出调度中心，随后快捷键失效。
- 临时运行追踪两次捕获进入处理阻塞 4537.6 / 4531.7 ms，结束时事件监听为禁用状态；窗口快照读取仅 1–5 ms。随后出现 `no-windows` 和 `close-timeout`。
- 独立实机测量：`hs.window.orderedWindows()` 为 4527.2 ms，`hs.window.get(id)` 为 1537.9 ms；系统 `hs.window.list()` 为 5.3 ms。线程采样指向全局枚举中的 `application_ishidden`、`application_allWindows` 和同步 AX 查询。
- 修复后按窗口所属 PID 查询，8 个实际窗口均按 ID 匹配成功，单次 3.6–8.8 ms。辅助功能查询设置每次 50 ms 超时，扫描目标应用窗口设 200 ms 截止时间，不查询其他应用。
- 回归测试让全局 AX 枚举直接失败：旧版控制器变红，新版三个套件通过（42 / 155 / 42 条断言）；同时覆盖只查询目标 PID 和目标应用无响应时结束操作。
- 发布前读取新版本的实际按键追踪：90 条 Tab 按下/松开事件，10 次 `committed`、1 次 `external-exit`；未记录到监听禁用、超时或超过 50 ms 的工作回调，记录的状态转换回调最大 22.8 ms。用户尚未明确确认主观交互体验；临时诊断已保存到本机临时目录并移除。

## 历史验证（2026-09-16）

日期：2026-09-16。

## 环境与结论

- macOS 27.0（26A428），Hammerspoon 1.1.1，单显示器，辅助功能权限已开启。
- 系统开启“按应用程序分组窗口”。实现对重叠缩略图计算可见区域，设置实际鼠标位置并发送悬停事件。
- 发现 AltTab 也使用 Command+Tab，曾抢占焦点。正式实机验收前已退出 AltTab；没有修改其配置。早期带 AltTab 的结果不计入最终验收。
- 退出 AltTab 后，两个 Chrome 同名空白窗口的 3 次切换成功；随后重新执行 20 次连续长按，20 次目标 ID 与实际焦点 ID 全部一致，调度中心均关闭。
- 短按回放触发应用切换，状态回到 idle；没有残留调度中心。
- 额外实机场景：Tab 后反引号返回、打开动画中提前松手均精确命中目标；Esc 取消后焦点保持在原窗口。
- 已安装到 `~/.hammerspoon/Spoons/MissionTab.spoon`，启动块已写入 Hammerspoon 配置并创建备份；已加载运行。

## 自动化验证

通过 `./scripts/test.sh` 在 Hammerspoon 的 Lua 运行时执行，无外部依赖。

| 范围 | 断言数 | 覆盖 |
| --- | --- | --- |
| 纯状态机 | 30 | 长短按边界、Tab 长按、双 Command 聚合、重复键、连续导航、反向、取消、迟到 key-up、其他组合键 |
| 真实控制器 + 模拟系统边界 | 30 | 合成事件标记、立即连续短按、提前松手、反向选择、目标失效、打开/关闭超时、进程替换、外部退出、鼠标接管、Secure Input、stop 清理、打开前取消竞态、打开中停用/重启、诊断结果隔离 |
| AX 适配与几何 | 21 | WindowManager/Dock 两种结构、同名窗口 ID、MRU 关联、动画稳定、缺失进程、分组窗口可见区域、完全遮挡时取消 |

## 已知边界

- 自动实机按键由 Quartz 合成事件驱动；安装后用户实机反馈“短按和长按都正常”，真实物理按键基础交互已确认。
- 多屏、其他 macOS 版本、系统进程实际重启、Secure Input 的真实密码场景尚未实机验证；相关状态清理由模拟边界测试覆盖。
- AXPress 在早期调试中没有建立可靠的精确选窗证据，未用于正式确认路径。
- 完全被其他缩略图遮挡、没有安全可见区域的目标会取消，不猜测点击位置。
- 0.1.0/0.1.1 测试中的鼠标接管会结束会话；0.1.2 已按用户反馈修正为保留 Command 释放监听。系统产生的零位移 mouseMoved 不视为主动鼠标操作。
- AltTab 等工具不能同时接管同一个 Command+Tab。安装 MissionTab 不修改其他工具的登录启动设置。

## 复现

`README.md` 包含安装、停用及诊断命令。实机驱动为 `scripts/live-smoke.lua`，支持连续轮数和额外交互场景。驱动会保留所有计时器并捕获异步异常；鼠标接管或调度中心残留时中止，不继续发送下一轮按键。

## 评审修复

需求评审发现：在已发出打开请求、AX 尚未出现时取消，会过早结束会话。已改为有界等待并关闭随后出现的调度中心；同样覆盖 stop/start 清理，避免重复 toggle。补充回归验证后全部自动断言通过。

项目知识服务在本轮不可用，未宣称完成依赖外部 RULE 的完整规范评审；本地需求、实现、测试及用户实机反馈已核对。

## 0.1.1 导航顺序

按用户反馈改为顺时针 Tab、逆时针反引号，以最近使用的其他窗口为初始项。新增圆周顺序、初始选择与空间顺序分离、循环、单窗口及动画期间缓存输入的验证；81 项自动断言通过。原实机连续测试记录对应 0.1.0；本轮不重复无关的连续切换压力测试。

## 0.1.2

修正鼠标移动导致会话提前结束的问题，并支持在遗留调度中心中按 Command+Tab 退出。按用户要求直接交付，未运行测试或实机验证；此前断言和压力测试数量不代表本版本验收。

## 0.1.3

初始定位改为动画中预定位和跟随，取消 250 ms 固定起步门槛；按用户要求未测试，实际延迟和手感由用户验证。

## 0.1.4

增加应用堆叠组的整体顺时针排序和组内从上到下访问；按用户要求未运行测试，由用户验证。

## 0.2.0 无鼠标交互

当前系统实机调研中，直接 AXPress 为 6/8 命中，退出后按窗口 ID 聚焦为 8/8 命中。严格无鼠标原型仅监听 keyDown、keyUp、flagsChanged，对鼠标 API 设置报错防护；初始选择、Tab、反引号、Shift+Tab、Esc、提前松手、短按、已有调度中心退出共 8 场景各两轮，16/16 通过，鼠标调用违规为 0。测试为合成键盘事件，非人工物理按键。

安装实现补全选中框尺寸跟随、停止和异常时覆盖层清理、目标无法解析时取消。按用户要求，安装版效果由用户试用；未运行旧测试或新增实机回归。原有控制器测试依赖鼠标悬停，不能作为 0.2.0 验证依据。多屏和各应用兼容性未全量覆盖。

## 0.2.1 鼠标接管

用户明确选择移动鼠标退出键盘选择、保留调度中心。实现只监听 mouseMoved，非零位移隐藏选中框；松开 Command 使用系统鼠标选择，再按导航键恢复键盘导航。未运行自动化或实机交互测试，由用户试用验证。

## 0.2.2 鼠标起点与单屏范围

增加按 AXDisplayID 筛选鼠标屏幕（缺少显示器 ID 时使用几何归属），以系统 AX 命中元素确定指向的缩略图；没有命中则从左上方组开始。鼠标接管后再次导航重建范围和起点。完成语法及静态检查，未运行自动化或实机回归，多屏与层叠窗口命中由用户验证。

## 0.2.3 与原生高亮实验

恢复 Command+Tab 长按初始 MRU 选择，候选仍限定鼠标屏幕；鼠标接管后使用鼠标起点。完成语法检查，交互由用户验证。

原生光标模块实验可调用隐藏/显示，系统原生悬停框可见，但调度中心内持续隐藏查询未通过，故未安装该实验。正式版本继续自绘框、不主动移动指针。实验过程中所有临时监听、定时器和光标操作均已结束，最终光标可见，原版运行状态 idle。

## 0.2.4 可见光标与原生高亮

用户确认改用直接移动可见光标，移除自绘框。复用原有安全悬停区域、事件标记和悬停等待，保留当前屏幕与最近窗口起点；鼠标主动接管不回移，键盘退出和停用清理恢复起始位置。完成语法与静态检查，本轮未运行实机交互，由用户验证。

## 0.2.5 退出落点

确认退出并等待焦点稳定后，读取实际前台窗口矩形，将光标移到窗口中心，同时防止完成清理再次回移。取消保持原有恢复规则。完成语法与加载检查，未运行实机交互测试，由用户验证。

## 0.2.6 提前居中

光标居中移到调度中心消失后的首个处理周期，目标聚焦后立即执行。150 ms 仅用于后续焦点诊断，不再延迟移动；每次会话只居中一次。完成语法与加载检查，未进行实机交互测试，由用户验证。

## 0.2.8 鼠标续接与快捷键透传

鼠标命中判断增加明确的命中标志，续接时仅在实际命中窗口的情况下计入首次导航方向；空白处不跳过起点。会话只拦截导航与取消，其他按键原样放行，相关状态机断言已更新。按用户约定未运行自动化或实机交互测试，完成语法与加载检查。

## 0.2.9 空白处选择最近窗口

鼠标续接未命中窗口时，按到候选缩略图矩形的最短距离选择，等距时按中心距离打破平局；不额外推进一项。候选仍限定鼠标屏幕。完成语法与加载检查，交互由用户验证，未运行实机测试。

## 0.2.10 入场悬停刷新

布局稳定后经过 `hoverDelay`，重新读取当前目标坐标并补发一次悬停事件。确认等待此次事件处理，鼠标接管则不补发。控制器模拟系统忽略早期悬停事件：修改前未刷新目标，修改后通过；这验证时序处理，不证明 macOS 原生轮廓的实机效果。

执行 `./scripts/test.sh`：状态机 38 项、控制器 34 项通过；几何测试仍在历史 MRU 断言 `recent other window chooses initial selection` 失败（当前 MRU 选择属于控制器，`MC.order` 仅负责空间排序），未修改该无关测试。控制器测试补齐现有屏幕、窗口查找与窗口矩形接口，并按当前鼠标接管及退出居中行为更新旧断言。未进行实机交互，轮廓显示仍需试用确认。

## 2026-09-16：0.2.11 统一调度中心入口

- 移除短按回放和长按门槛；状态机 36 条、模拟控制器 35 条断言通过，覆盖快速释放 Command、晚到的 Tab 松开与确认目标。
- `./scripts/test.sh` 的几何测试仍在 `recent other window chooses initial selection` 失败；使用改动前 HEAD 的测试和几何模块复现相同失败，属于既有问题。
- 保留布局稳定、悬停补发及确认等待，真实动画响应速度由用户试用判断。

## 2026-09-16：0.2.12 松键立即请求切换

- 状态机 36 条、模拟控制器 40 条断言通过，新增覆盖入场首帧确认、悬停未就绪、退出动画期间请求聚焦、退出焦点校正及立即接收下一次手势。
- 几何测试仍为上一轮已复现的既有失败。
- 当前桌面单次短按实测：`matched=true`、`reason=committed`、调度中心已关闭，程序记录从首次 Tab 按下至完成约 241 ms。该计时不是逐帧测量，单次样本不代表连续操作或其他系统环境。

## 2026-09-16：0.2.13 动画期间连续输入

- 用户确认保留动画、操作不等动画；不调整系统动画设置，不区分长短按。
- 状态机 36 条、模拟控制器 56 条断言通过：包括移动中的入场布局、退出期间多手势、后续仍按住 Command 的导航归属、取消、失败清理和匹配的松键。几何测试保留既有失败记录。
- 实机通过控制器入口以 100 ms 间隔输入三次完整手势，三次均 `committed` 且目标与实际焦点匹配；最终调度中心关闭、会话空闲。各次记录耗时约 211 / 189 / 399 ms（包含系统执行及排队），未做逐帧延迟测量。

- Spec 评审发现并修复取消一个排队手势连带丢弃后续独立手势的问题；新增对应回归与睡眠全局中断测试。Standards 所需项目知识 RULE 服务不可用，未宣称完成规范全量评审。

## 2026-09-16：0.2.14 恢复短按原生切换

- 用户在确认动画输入问题修复后要求恢复短按直接切换；恢复默认 180 ms 门槛，保留动画中选择、确认及输入队列。
- 状态机 42 条、模拟控制器 64 条断言通过，覆盖短按原生回放、连续短按、反向及修饰键保留、退出动画期间短按与后续长按的衔接。原动画路径测试使用 `holdDelay=0` 继续回归。几何测试仍为既有失败。
- 实机 smoke 本次短按记录 native-replay 但前后焦点相同、长按记录 cancelled，未取得有效的实机切换通过证据，不据此宣称实机验证通过。

## 2026-09-16：0.2.15 半透明高亮实验

- 状态机 42 条、模拟控制器 72 条断言通过，新增遮罩随目标移动、鼠标穿透配置、鼠标接管与键盘恢复、松键/取消/停用清理覆盖。几何测试仍是既有失败。
- 在当前桌面通过控制器入口打开调度中心，隔离临时实例的键盘监听及健康检查后截图检查：半透明蓝色填充位于选中缩略图内，原生蓝色边框仍可见，状态为 navigating。首次未隔离的预览会话被取消，未产生截图。
- 本次没有增加额外动画或切换等待；视觉样式为供用户试用的实验版本。

## 2026-09-16：0.2.16 鼠标悬停遮罩

- 状态机 42 条、模拟控制器 79 条断言通过，新增鼠标接管、跨缩略图移动、空白处隐藏、重新悬停、确认清理与入场动画期间更新覆盖；确认高亮更新不移动光标、不补发悬停事件。几何测试仍为既有失败。
- 本次实机预览会话记录 cancelled，未取得有效的鼠标遮罩显示截图，不作为实机通过证据；沿用既有遮罩绘制，效果由用户试用确认。

## 2026-09-16：0.2.17 首次进入抖动并退出

- 复现：临时启动追踪记录 223 次屏幕通知，显示器布局只有一个唯一值；第一次 hover 后出现 3 次 cancelled，未收到取消按键。原 screenWatcher 无条件取消活动会话。
- 新增回归在修复前失败：`unchanged screen geometry notifications do not cancel first entry`；修复后状态机 42 条、控制器 85 条断言通过，覆盖重复通知、真实位置变化、显示器枚举顺序与断开。几何测试仍为既有失败。
- 修复后同样的实机启动追踪收到 210 次屏幕通知，取消次数为 0，按住时 navigating，目标保持同一个，松键后 committed/matched=true。
- 安装修复并实际执行 `hs.reload()` 后，对新加载环境的首次进入再测：223 次屏幕通知、0 次取消；按住时 navigating、目标稳定，松键后 committed/matched=true，最终 idle。临时插桩未写入生产代码。


## 2026-09-16：入口状态与回归基线收敛

- 去除入口对 0.5 秒缓存的依赖，在异步处理阶段读取现场状态；短按回放通过零延迟定时器调度，避免在键盘回调中读取 AX，也不等待下一次 30 ms 轮询。
- 新增手动打开、手动关闭、长按期间打开、短按退出已有调度中心及停止后迟到回调的回归。
- 当前屏幕没有候选窗口时以 `no-windows` 结束本次操作，不永久暂停；调度中心未出现仍保留超时暂停。
- 修正几何测试的历史 MRU 预期，MRU 继续由控制器回归覆盖。
- `./scripts/test.sh`：状态机 42、控制器 93、AX/几何 21 项通过。未运行桌面交互测试。

## 2026-09-16：安装配置幂等性

- 安装脚本保留已有 MissionTab 引用，不再重复追加手动启动配置；注释或间接加载引用同样保留，并提示用户检查。
- 在临时目录验证空配置、手动配置、注释引用三种场景，各执行两次安装；配置保留、重复执行、安装内容和旧版本备份均通过。没有操作真实 Hammerspoon 安装。


## 2026-09-16：0.2.18 性能收敛

- 对照基线 `63cf020` 与当前实现，用现有控制器 fixture 在内存中对 AX 快照、`MC.point`、canvas `frame`/`show` 调用插桩；未写入生产逻辑，也未启动桌面事件监听。
- 每场景执行 1,000 次处理。静止与移动场景均在入场和补发悬停完成后采样；使用三个窗口，移动场景将横向位移限制在屏幕内以保持整个采样阶段导航有效。

| 场景与指标 | 基线 | 优化后 |
| --- | --- | --- |
| 空闲健康检查：AX 快照 | 1,000 | 0 |
| 静止选择：canvas frame 更新 | 1,000 | 0 |
| 静止选择：canvas show | 1,000 | 0 |
| 移动目标：安全悬停点计算 | 2,000 | 1,000 |
| 移动目标：canvas frame 更新 | 1,000 | 1,000 |
| 活动导航：AX 快照 | 1,000 | 1,000 |

- 保留活动期间的新鲜 AX 快照和移动目标的绘制频率；显示器 ID 从每窗口读取改为每屏每快照读取一次。
- `./scripts/test.sh`：状态机 42、控制器 96、AX/几何 22 项，共 160 项通过；新增空闲不枚举、静止不重绘、移动目标仍命中安全悬停点及显示器属性读取次数的回归。
- 本轮按当前 README、用户指出的入口边界与性能优化目标核对改动；外部项目 RULE 服务仍不可用，不宣称完成全量规范审查。
- 本轮没有安装、重载 Hammerspoon 或执行真实键鼠 smoke；模拟调用次数不能换算成实机 CPU、帧率或端到端延迟提升。


## 2026-09-16：0.2.19 关闭窗口后保留调度中心

- 本机旧版本状态记录 `lastResult.reason = target-disappeared`，与控制器选中窗口消失后主动取消的路径一致。
- 回归覆盖关闭选中窗口、关闭其他窗口后继续导航、入场和稳定导航期间关闭全部窗口、窗口恢复后继续导航及松键确认。
- `./scripts/test.sh`：状态机 42、控制器 110、AX/几何 22 项，共 174 项通过。未执行真实关闭窗口或退出应用的桌面测试。
- 外部项目 RULE 服务不可用，不宣称完成全量规范审查。


## 2026-09-16：0.2.20 重叠缩略图不再触发退出

- 本机 0.2.19 的最近退出原因是 `target-occluded`；此前仅修复了 `target-disappeared` 路径，未覆盖几何重叠触发的主动取消。
- 用真实 `MC.point` 计算重叠窗口的可见区域，新增控制器回归在修复前失败：`overlapping thumbnails must not close overview or block entry stabilization`。
- 删除悬停失败时取消整个会话的行为，同时去除调用方的提前返回，避免阻塞入场布局稳定判定。
- 覆盖入场/稳定导航期间完全重叠、持续重叠超过入场超时、不移动到不安全位置、恢复原生悬停及按窗口 ID 确认。
- `./scripts/test.sh`：状态机 42、控制器 118、AX/几何 22 项，共 182 项通过。未执行实机键鼠回归，不将模拟测试结果表述为所有异常退出均已消除。
- 外部项目 RULE 服务不可用；按用户反馈及当前交互核对本轮变更。


## 2026-09-16：0.2.21 统一窗口环形顺序

- 按用户新要求，移除按应用/Space 合组及组内从上到下的排序；所有缩略图中心统一参与顺时针角度排序，反向使用同一数组逆序。
- 几何回归覆盖左侧堆叠窗口顺时针应从下到上、完整环的正反方向和完全重合窗口的确定顺序；原有最近窗口起点与控制器回归保留。
- `./scripts/test.sh`：状态机 42、控制器 118、AX/几何 33 项，共 193 项通过；未执行实机键鼠回归。
- 外部项目 RULE 服务不可用；按用户确认的新顺序与 README 核对变更。

## 2026-09-16：0.2.22 逐行导航

- 按用户要求，将顺时针循环改为从左到右、从上到下的逐行循环；以缩略图中心和半个最小高度容差识别同行，反向键使用完整逆序。最近窗口起点由控制器保留。
- `./scripts/test.sh` 全部通过：状态机 42 条、控制器 118 条、几何 42 条，共 202 条断言。覆盖多行布局、同行尺寸及位置差异、垂直堆叠、循环反向、重合窗口及空/单窗口。
- `git diff --check` 通过；未进行实机键盘交互验证。外部项目知识服务未提供可用入口，依据当前用户要求及仓库文档完成实现。

## 2026-09-16：0.2.23 逐列导航

- 用户澄清需要逐列导航：每列从上到下，再从左列进入右列。以缩略图中心与半个最小宽度容差识别同列，反向导航使用完整逆序。
- `./scripts/test.sh` 全部通过：状态机 42 条、控制器 118 条、几何 42 条，共 202 条断言；覆盖两行三列的明确逐列顺序、同列宽度与横向偏移差异、堆叠及循环反向。
- `git diff --check` 通过，未执行实机键鼠回归。外部项目知识服务无可用入口，依据用户澄清及仓库文档实现。

## 2026-09-16：0.2.24 遮罩同步与意外退出路径

- 用户修正后的 GIF 共 204 帧、约 13.59 秒，显示原生窗口边框已切换时，遮罩仍处于窗口之间；与早先仅 3 帧的错误附件区分。
- 实机反向切换探针中，移动画布版本截图出现遮罩偏移，画布回读几何与目标基本一致（存在约 1–2 点取整）；说明不能仅靠回读矩形证明屏幕显示同步。改成固定屏幕画布、只更新绘制矩形后，在反向切换后静止 0.5 秒的截图中确认遮罩与原生边框对齐。快速连续操作仍需用户试用；不把稳定截图称为逐帧同步证明。
- 控制器回归重现两个额外退出路径：持续移动的入场布局超过 1.5 秒触发主动关闭；事件监听停用后的健康检查直接取消。分别修复为有可用窗口时固定顺序、恢复监听并核对 Command 状态。无法确认用户单次自动退出具体命中了哪一路径。
- `./scripts/test.sh`：状态机 42、控制器 132、几何 42，共 216 条断言通过。新增反引号反向选择/遮罩一致、可见画布无原生移动缩放、多屏负坐标、入场超时继续导航及确认、监听恢复和丢失松键恢复。
- 实机探针使用临时控制器，结束后恢复已安装实例；临时脚本及含个人桌面内容的截图未加入仓库。外部项目知识服务无可用入口。

## 2026-09-16：0.2.25 移除遮罩边距

- 按用户要求移除四周 2 点内缩，直接使用缩略图完整矩形；保留现有透明度、圆角与固定画布绘制。
- 更新既有位置断言，并核对完整宽高；`./scripts/test.sh` 状态机 42、控制器 132、几何 42，共 216 条断言通过。`git diff --check` 通过，未重复实机键鼠测试。
