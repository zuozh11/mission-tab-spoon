# 版本记录

## 未发布

- 修正导航规则为按缩略图中心从左到右循环，移除重叠分列；中心横坐标相同时从上到下访问。两张反馈截图分别按 `1 → 3 → 2 → 4`、`1 → 4 → 3 → 2` 导航。

## 0.2.29 — 2026-09-20

- 修复上下错开的窗口被分入错误列的问题：按缩略图共同横向重叠区间识别列，列内从上到下导航，截图中的布局按 `1 → 3 → 2 → 4` 循环。
- 保留完整逆序导航，避免相邻窗口依次重叠时把多个列串成一列。
- 补充截图布局与重叠链回归，三个自动化测试套件共 253 项断言通过。

## 0.2.28 — 2026-09-18

- 修复进入调度中心时把窗口堆叠顺序当作最近使用顺序的问题：记录前台应用的真实焦点变化，支持同一应用内切换窗口；确认或取消后记录实际焦点，预览不污染历史。
- 只监听前台应用，保留有时限的辅助功能查询；启动后尚无使用记录的窗口以系统顺序补充。

## 0.2.27 — 2026-09-18

- 修复切换期间同步遍历所有应用的辅助功能窗口，导致主线程阻塞数秒、键盘监听失效和进出调度中心卡顿的问题。最近窗口顺序改从系统窗口列表读取，确认时只查询目标窗口所属应用，并限制辅助功能查询等待。

## 0.2.26 — 2026-09-18

- 修复调度中心关闭超时后插件持续暂停的问题：清理本次操作并保留超时记录，下一次快捷键重新检查当前调度中心状态，无需手动重启。
- 超时仍丢弃排队手势，不自动重复切换调度中心。

## 0.2.25 — 2026-09-16

- 移除遮罩四周的 2 点内缩，按缩略图完整位置和尺寸绘制。

## 0.2.24 — 2026-09-16

- 遮罩使用固定在当前屏幕上的透明画布，仅更新内部矩形，避免反向等快速切换时画布移动、缩放造成遮罩偏移。
- 入场已有可用窗口但布局迟迟未稳定时，固定当前导航顺序并继续跟随几何，不因稳定等待超时关闭调度中心。
- 键盘监听短暂停用时恢复监听并核对 Command 的实际状态，仍按住时保留会话，已松开时正常确认。

## 0.2.23 — 2026-09-16

- 按用户澄清修正为逐列导航：每列从上到下，再进入右侧下一列；反向键使用完整逆序。
- 保留最近窗口起点，并支持同列缩略图的尺寸和横向位置差异。

## 0.2.22 — 2026-09-16

- 导航改为从左到右、从上到下的逐行循环，支持同行缩略图的尺寸和位置差异；反向键使用完整逆序。
- 保留最近窗口起点和重合窗口的确定顺序。

## 0.2.21 — 2026-09-16

- 所有窗口统一按缩略图中心的空间角度顺时针导航，移除堆叠应用组内从上到下的特殊顺序；反向导航严格使用逆序。
- 保留最近窗口起点；位置重合的窗口按 ID 确定顺序。

## 0.2.20 — 2026-09-16

- 修复缩略图重叠、没有安全悬停位置时主动退出调度中心的问题；保留键盘选择和入场布局处理，仅暂停光标移动。
- 可见区域恢复后继续原生悬停；完全遮挡时仍可松开 Command 按窗口 ID 确认。

## 0.2.19 — 2026-09-16

- 修复仍按住 Command 时，关闭选中窗口导致调度中心自动退出的问题；窗口消失后继续选择剩余窗口并更新导航顺序。
- 已有窗口全部关闭时保留调度中心并交给鼠标操作；快捷键透传、松键确认及 Esc 取消保持原行为。

## 0.2.18 — 2026-09-16

- 手动开关调度中心后按 Command+Tab，按现场状态选择退出或原生切换，不再受 0.5 秒缓存影响；状态检查在异步处理阶段执行。
- 当前屏幕无候选窗口时结束本次操作，不持续暂停插件；保留真正打开失败时的暂停处理。
- 安装脚本保留已有 MissionTab 配置或引用，避免重复追加启动块。
- 移除空闲时 AX 枚举；静止遮罩不重复更新画布，移动目标的悬停点只计算一次，显示器 ID 每个快照按屏读取一次。
- 修正历史几何测试预期并补充回归，三个自动化套件全部通过。性能数据来自模拟调用计数，不代表实机延迟或 CPU 降幅。

## 0.2.17 — 2026-09-16

- 修复重载后首次进入调度中心时，被密集屏幕通知反复取消并自动退出的问题。
- 仅在显示器数量、标识、位置或完整尺寸发生变化时取消；忽略 Dock 显隐等引发的布局未变通知。

## 0.2.16 — 2026-09-16

- 鼠标接管后也显示半透明高亮，跟随鼠标当前屏幕上的实际悬停窗口；空白处隐藏。
- 入场动画期间同样更新，不移动鼠标或改动原生确认、点击行为。

## 0.2.15 — 2026-09-16

- 实验：保留原生高亮边框，为键盘选中的缩略图叠加 20% 透明度的浅蓝遮罩，跟随入场动画与导航选择。
- 遮罩不接管鼠标输入；确认、取消、鼠标接管、停用及异常退出时清理，不添加动画等待。

## 0.2.14 — 2026-09-16

- 恢复短按 Cmd+Tab 直接原生应用切换，默认 `holdDelay` 为 180 ms；设为 0 仍可使用统一调度中心入口。
- 保留动画期间选择、确认与连续输入修复；退出期间的短按按序回放，且恢复后续手势仍按住的 Command 状态。

## 0.2.13 — 2026-09-16

- 保留系统动画，统一处理所有 Cmd+Tab 手势，不区分长短按。
- 已松键的手势尚未退出时，继续接收后续手势的选择、确认和取消，退出后顺序接续，不再丢失动画期间的输入。
- 验证入场布局持续移动时仍可更新选择并确认；退出失败会清理待处理手势。

## 0.2.12 — 2026-09-16

- 松开 Command 后，目标一旦可用就请求退出与聚焦，移除确认前的布局稳定及悬停等待。
- 退出后校正焦点并立即释放会话，移除占住后续输入的 150 ms 诊断等待。
- 保留按住 Command 时的原生悬停反馈；系统动画仍由 macOS 控制。

## 0.2.11 — 2026-09-16

- 移除短按原生应用切换及 `holdDelay` 门槛，首次 Cmd+Tab 按下即进入调度中心路径。
- 提前松开 Command 仍确认所选窗口；保留布局稳定与悬停等待，供响应速度对比测试。

## 0.2.10 — 2026-09-16

- 布局稳定并等待悬停间隔后，补发一次原生悬停事件，处理入场阶段光标已定位但选中轮廓未刷新的情况。
- 鼠标接管后停止补发；提前松开 Command 时等待补发及悬停处理后确认。

## 0.2.9 — 2026-09-16

- 鼠标接管后从空白处按导航键，直接选择当前屏幕内离鼠标最近的窗口，按到缩略图边缘的距离判断。
- 鼠标已悬停窗口时，继续保持跳到下一项或上一项的行为。

## 0.2.8 — 2026-09-16

- 鼠标接管后按 Tab，从悬停窗口直接跳到下一项；反向键跳到上一项。空白处仍从左上方起步。
- 非导航快捷键原样透传，包括 Command+W、Command+Q；不再拦截其按下或释放。
- 更新相关状态机断言，交互由用户试用验证。

## 0.2.7 — 2026-09-16

- 完善 README：下载安装、仓库更新、键鼠操作、屏幕范围、配置、排查与回退说明。
- 补充发布下载入口与兼容性说明；运行行为与 0.2.6 相同。

## 0.2.6 — 2026-09-16

- 检测到调度中心退出并聚焦目标时立即居中光标，移除居中前的 150 ms 等待。
- 焦点诊断仍等待稳定，不再延迟或重复移动光标。

## 0.2.5 — 2026-09-16

- 确认退出调度中心后，光标移动到实际打开窗口的中心，不再回到起始位置。
- Esc 取消与停用清理保持原有恢复规则，交互由用户试用验证。

## 0.2.4 — 2026-09-16

- 直接移动可见光标，使用系统原生悬停框，移除自绘框。
- 保留当前屏幕、最近窗口起点及鼠标接管规则。
- 纯键盘退出恢复光标位置；鼠标主动接管后保留用户位置。
- 不包含隐藏光标模块，交互由用户试用验证。

## 0.2.3 — 2026-09-16

- Command+Tab 长按进入默认使用 Tab 模式，以当前鼠标屏幕内最近使用的其他窗口为起点。
- 鼠标接管后再次导航仍按鼠标目标定位，保留单屏范围。
- 隐藏光标与原生焦点框方案未通过调度中心内的持续隐藏验证，不包含在此版本。

## 0.2.2 — 2026-09-16

- 键盘选择限定鼠标所在屏幕，起点优先使用鼠标指向的调度中心窗口。
- 没有命中窗口时，从布局左上方开始顺时针循环。
- 鼠标接管后再次导航，按当前鼠标屏幕和目标重建起点，不再沿用最近窗口或旧索引。
- 按用户要求直接安装，交互与多屏效果由用户验证。

## 0.2.1 — 2026-09-16

- 鼠标移动时隐藏键盘选中框并交还鼠标选择，保留调度中心。
- 松开 Command 确认鼠标选择，再按导航键恢复键盘模式；不主动移动鼠标。
- 按用户要求快速安装，交互由用户验证。

## 0.2.0 — 2026-09-16

- 改用自绘选中框和退出后按窗口 ID 聚焦。
- 完全移除运行时鼠标位置读写、鼠标事件发送与监听；不再支持鼠标接管键盘选择。
- 保留长短按、最近窗口、分组顺逆序、取消和提前松手。
- 临时原型已完成两轮共 16 次实机合成按键验证；安装版由用户试用，未执行新增回归。

## 0.1.4 — 2026-09-16

- 应用堆叠组作为整体参与顺时针循环。
- 组内 Tab 按屏幕位置从上到下访问，反引号完整反向。
- 初始仍定位最近使用的其他窗口。
- 整理仓库首页、安装说明、贡献指南和根目录许可证。

## 0.1.3 — 2026-09-16

- 移除初始定位前固定的 250 ms 等待。
- 缩略图出现后立即预定位，动画中持续跟随。
- 布局稳定后固定导航顺序，保留确认等待。

## 0.1.2 — 2026-09-16

- 修复移动鼠标后提前结束会话、松开 Command 无法退出的问题。
- 鼠标选择后仍可回到键盘导航。
- 无活动会话时，支持用 Command+Tab 退出已打开的调度中心。

## 0.1.1 — 2026-09-16

- Tab 改为顺时针导航，反引号改为逆时针。
- 最近使用顺序只决定初始窗口，不再决定整个导航顺序。

## 0.1.0 — 2026-09-16

- 实现短按原生应用切换、长按调度中心窗口切换。
- 适配 macOS 27 的 WindowManager 辅助功能结构。
- 支持提前松手、取消、分组窗口可见区域定位及异常清理。
- 提供安装脚本、诊断入口和开发测试工具。

以上条目记录实现演进，不表示每个版本都有公开发布记录。
